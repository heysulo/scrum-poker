// ------------------------------------------------------------------
// MOCK MODE CONFIGURATION
// The app is currently running in Mock Mode for local testing.
// No actual Firebase connection is required.
// ------------------------------------------------------------------

// We export a dummy object so imports in other files don't break.
export const db = { type: 'mock' };
